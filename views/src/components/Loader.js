import React from 'react';
import '../assets/css/Loader.css';  // Import the CSS file for styling the loader

const Loader = () => (
  <div className="loader-container">
    <div className="loader"></div>
  </div>
);

export default Loader;
